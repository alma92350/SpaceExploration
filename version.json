{
  "version": "2.6.2",
  "saveVersion": "v2",
  "notes": "Base trade: import and export are now mutually exclusive per commodity, so a finished good can no longer be imported from one colony and exported back to another in the same loop (wasting freight). Finished goods default to import; click a good's export chip to flip it the other way (locked direction shows ⇄). Raw materials still export as before."
}
