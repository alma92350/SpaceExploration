{
  "version": "2.78.1",
  "saveVersion": "v2",
  "notes": "Internal: code-smell cleanup — removed a dead duplicate huntPirates() declaration, fixed freshState() to set S.eink directly, and stripped a dozen unrelated save-migration checks out of the hot-path reserveOf() function. No gameplay changes."
}
