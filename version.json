{
  "version": "2.5.1",
  "saveVersion": "v2",
  "notes": "Fix: opening a freshly built base's Import/Export tab no longer crashes (TypeError on b.trade.cols). The trade predicates are now null-safe and new bases initialize their trade routes on creation, so the tab works immediately without needing a reload."
}
